# Environment / 环境

Run `01_discover_environments.sh` read-only, select explicit executables in `config/environments.tsv`, and verify versions before analysis. Do not install or upgrade shared environments implicitly.

只读运行环境发现脚本，在配置中记录绝对可执行文件和版本；不得静默修改共享环境。
